"""base.py — placeholder for the Store protocol; no interface has been written yet.

The module exists because the planned layout names it and the scaffold test
imports it. ``SqliteStore`` is still the only implementation, and a protocol
extracted from a single implementation describes that implementation rather than
the seam — so this stays empty until a second backend says where the seam is.
"""
